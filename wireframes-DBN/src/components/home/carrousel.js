const carrousel = () => {
    const _carrousel = document.createElement('div');
    _carrousel.className = "w-full h-80 bg-white relative"
    const wrapper = document.createElement('ul')
    

    return _carrousel;
};

export default carrousel;